import UploadFile from "../../components/Upload/UploadFile";

// Home component
const Home = () => {
    return (
        <div>
            <UploadFile />
        </div>
    );
};

export default Home;